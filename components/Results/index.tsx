import styles from "./Results.module.scss";

export default function Results() {
  return (
    <section className={styles.results}>
      <h2 className={styles.results__title}>Good job!</h2>
    </section>
  );
}
